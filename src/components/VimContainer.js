import React, { Component } from 'react'
import styled from 'styled-components'

const VimContainer = styled.div`
  display: flex;
  flex-wrap: wrap;
  border: 3px solid #000;
  overflow: hidden;
`

export default VimContainer
